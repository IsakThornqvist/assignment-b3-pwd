## LocalStorage
Used to store the nickname in the local storage to make sure the user gets the correct experience on the chat application when logging back in etc.

## Result
A minimal component that handles the saving of information to the local storage